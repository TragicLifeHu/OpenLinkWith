apply from: configFile('android-library-config.gradle')

dependencies {
    implementation project(':base-prefs')
    implementation project(':browser-resolver')
}

android {
    namespace 'com.tasomaniac.openwith.browser.preferred'
    buildFeatures {
        viewBinding true
    }
}